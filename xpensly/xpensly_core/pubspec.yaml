name: xpensly_core
description: Pure Dart expense splitting engine — split calculations, debt simplification, multi-currency support, and pluggable adapters.
version: 1.0.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  collection: ^1.17.0

dev_dependencies:
  test: ^1.24.0
  lints: ^3.0.0
