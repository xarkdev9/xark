name: xpensly_ui
description: Flutter widgets for Xpensly expense splitting — composable, themed, drop-in components.
version: 1.0.0

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.10.0'

dependencies:
  flutter:
    sdk: flutter
  xpensly_core:
    path: ../xpensly_core

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
