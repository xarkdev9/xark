/// Device management — Phase 2.
/// Device registry, linking protocol, and multi-device key rotation.
/// See CLAUDE.md §Multi-Device Architecture.
library;

// TODO(phase2): Implement DeviceRegistry, DeviceLinkingProtocol,
// and KeyRotationService.
// Dependencies: crypto layer (key_store.dart), transport layer.
